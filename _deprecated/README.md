# _deprecated/

Vast.ai-эпоха скриптов. Не работают с текущим VDS 193.233.87.211.

Используй [`../deploy_vds.sh`](../deploy_vds.sh). См. `./deploy_vds.sh --help`.

История оригиналов — в `git log --follow <file>`.
